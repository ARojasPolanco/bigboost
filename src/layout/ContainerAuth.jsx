const ContainerAuth = ({ children }) => {
  return (
    <section className="font-montserrat min-h-screen bg-purple-bg text-white grid justify-items-center items-center sm:grid-cols-[auto_auto] gap-12 sm:justify-center">
      {children}
    </section>
  )
}

export default ContainerAuth;
